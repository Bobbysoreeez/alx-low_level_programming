#include <stdio.h>

/**
 * main - this program prints out the alphabet in reverse
 * Return:Always 0
 */
int main(void)
{
	char l;

for (al = 'z'; al >= 'a'; al--)
putchar('l');

putchar('\n');
return (0);
}
