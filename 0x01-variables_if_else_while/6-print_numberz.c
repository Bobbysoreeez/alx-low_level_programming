#include <stdio.h>
/**
 * main - prints the numbers base ten incluuding zero
 * Return: Always 0
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);

	printf("\n");

	return (0);
}
