#include "main.h"
/**
 * _isdigit- checks for a digit through 0 to 9
 * @c: character to be checked
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	int i;

	i = 0

	while (i <= 9)
	{
	if (c >= 0 && c <= 9)
	return (1);

	else
	return (0);

	i++;
	}
}


