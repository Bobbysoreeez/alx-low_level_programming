#include "main.h"
/**
 * main - program that checks for lowercase letters
 * Return: 1 success
 */
int main(void)
{
	char chr = 'c';

	if (islower(chr))
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
