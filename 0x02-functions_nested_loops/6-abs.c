#include "main.h"
/**
 * _abs -function that computes the absolute value of interger
 * @n: the int to check
 * Return: 0 success
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

	return (0);
}
